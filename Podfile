# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'pageboy_demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Pageboy'
  # Pods for pageboy_demo

  target 'pageboy_demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'pageboy_demoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
